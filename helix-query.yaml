indices:
  english: &default
    include:
      - /**
    exclude:
      - /en/snippets/**
    target: /query-index.json
    properties:
      title:
        select: main h1:first-of-type
        value: |
          textContent(el)
